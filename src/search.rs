

pub struct AlphaBeta;

impl AlphaBeta {
    fn alpha_beta_search(board: Board, alpha: i32, beta: i32, depth_left: i32, maximizing_player: bool) -> i32 {
        if maximizing_player {
            let mut max_eval = -i32::MAX;
            for m in moves {
                
            }

            max_eval
        } else {
            let mut min_eval = i32::MAX;
            for m in moves {
              
            }
        }
    }
}